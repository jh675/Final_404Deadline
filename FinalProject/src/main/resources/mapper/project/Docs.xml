<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.example.demo.project.docs.mapper.DocsMapper">
 
	 <select id="selectAll">
		 SELECT *
		 FROM   document
		 where  prj_id = #{prjId}
		 ORDER BY id desc
	</select>
 </mapper>