<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.project.notice.mapper.NoticeMapper">

<select id="selectAll" resultType="com.example.demo.project.notice.service.NoticeVO">
    SELECT 
        n.*, 
        u.name || '(' || u.login || ')' AS memName
    FROM notice n
    LEFT JOIN users u ON n.mem_id = u.id
    WHERE n.prj_id = #{prjId}
    ORDER BY n.is_fixed DESC, n.id DESC
</select>
<insert id="insert">
<selectKey keyProperty="id" resultType="long" order="BEFORE">
        SELECT news_seq.nextval FROM DUAL
    </selectKey>
insert into notice(
		id, 
		prj_id, 
		mem_id, 
		title, 
		description,
		is_fixed
		)
values(
		#{id}, 
		#{prjId}, 
		#{memId}, 
		#{title}, 
		#{description},
		#{isFixed}
)
</insert>

<select id="selectOne" resultType="com.example.demo.project.notice.service.NoticeVO">
 
 select *
 from notice
 where id = #{id}

</select>

<update id="update">
update notice
set 
    prj_id = #{prjId}, 
    mem_id = #{memId}, 
    title = #{title}, 
    description = #{description},
    is_fixed = #{isFixed} 
where id = #{id}
</update>

<delete id="delete">
delete from notice
where id = #{id}
</delete>

</mapper>