<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.example.demo.project.calender.mapper.CalenderMapper">
 
 <select id="selectAll" resultType="calenderVO">
  select * from calender
  
  <!-- 일정 검색조건 (일정유형 기준) -->
  <where>
   and mem_id = #{memId}
    <if test="typeCd != null and typeCd !=''">
     and type_cd = #{typeCd}
    </if>
     <if test="keyword != null and keyword !=''">
        AND (
                PLACE LIKE '%' || #{keyword} || '%'
                OR CAL_TEXT LIKE '%' || #{keyword} || '%'
            )
     </if>
  </where>
  
 </select>
   <select id="selectOne" resultType="calenderVO">
     select * from calender
     where id = #{id}
   </select>
   
   <insert id="insert">
     insert into calender ( 
     						id,
                            MEM_ID, 
                            PLACE, 
                            CAL_TEXT, 
                            TYPE_CD, 
                            SHARING_CD, 
                            STATE_CD, 
                            COLOR_TAG_CD,
                            CAL_TYPE,
                            CAL_START, 
                            CAL_END)
     values (
     		CAL_SEQ.nextval,
     		#{memId},
            #{place},
            #{calText},
            #{typeCd},
            #{sharingCd},
            '01STATE',
            #{colorTagCd},
            #{calType},
            #{calStart},
            #{calEnd})
   </insert>
   
   <update id="update">
	update calender
	set  PLACE = #{place},
         CAL_TEXT = #{calText},
         SHARING_CD = #{sharingCd},
         TYPE_CD = #{typeCd},
         CAL_TYPE = #{calType},
         CAL_START = #{calStart},
         CAL_END = #{calEnd}
	where id = #{id}
   </update>
   
   <delete id="delete">
    DELETE FROM calender 
    WHERE id = #{id}
   </delete>
   
<select id="getList" resultType="calenderVO">

	select * from calender

	<where>
		and mem_id = #{memId}
		<if test="typeCd != null and typeCd != ''">
			and type_cd = #{typeCd}
		</if>
		<if test="keyword != null and keyword != ''">
			and (
			place like '%' || #{keyword} || '%'
			or cal_text like '%' || #{keyword} || '%'
			)

		</if>

	</where>

</select>
   


<!-- 1시간 전 알림용 -->
<select id="findByCalStartBetween" resultType="calenderVO">
    SELECT
        ID AS id,
        CAL_TEXT AS calText,
        get_sub_code_name(CAL_TYPE) AS calType,
        get_sub_code_name(TYPE_CD) AS typeCd,
        CAL_START AS calStart
    FROM CALENDER
    WHERE CAL_START BETWEEN #{start} AND #{end}
</select>
   
 </mapper>
