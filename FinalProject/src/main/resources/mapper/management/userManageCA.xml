<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper	namespace="com.example.demo.management.userManage.mapper.CAUserManageMapper">

	<select id="selectAll">
		SELECT 	 ID
				,BIZ_NO
				,get_bizno_to_compname(BIZ_NO) "COMP_NM"
				,LOGIN
				,NAME
				,TEL
				,EMAIL
				,HIRE_DATE
				,GENDER_CD
				,get_sub_code_name(GENDER_CD)"GENDER_NM"
				,ADMIN_CD
				,get_sub_code_name(ADMIN_CD) as "ADMIN_NM"
				,STATUS_CD
				,get_sub_code_name(STATUS_CD) "STATUS_NM"
				,CREATED_ON
				,PW_UPDATED_ON
				,PRJ_MANAGER_CD
				,get_sub_code_name(PRJ_MANAGER_CD) "PRJ_MANAGER_NM"
				,MCP_CD
				,get_sub_code_name(MCP_CD) "MCP_NM"
		FROM 	 users
		<where>
		    BIZ_NO = #{bizNo}
		    
		    <if test="prjManagerCd != null and prjManagerCd != ''">
		        AND PRJ_MANAGER_CD = #{prjManagerCd}
		    </if>
		    
		    <if test="keyword != null and keyword != ''">
		        <if test="searchType == 'login'">
		            AND LOGIN LIKE '%' || #{keyword} || '%'
		        </if>
		        <if test="searchType == 'name'">
		            AND NAME LIKE '%' || #{keyword} || '%'
		        </if>
		        <if test="searchType == 'email'">
		            AND EMAIL LIKE '%' || #{keyword} || '%'
		        </if>
		    </if>
		</where>
		ORDER BY ADMIN_CD, PRJ_MANAGER_CD, ID
	</select>
	
	<insert id="insertUser" statementType="CALLABLE">
	    { CALL PROC_USER_INSERT(
	        #{bizNo},
	        #{login},
	        #{password},
	        #{name},
	        #{tel},
	        #{email},
	        #{adminCd},
	        #{statusCd},
	        #{mcpCd},
	        #{prjManagerCd},
			#{hireDate, jdbcType=DATE},
        	#{genderCd},
        	
	        #{result, mode=OUT, jdbcType=VARCHAR},
	        #{id, mode=OUT, jdbcType=NUMERIC, javaType=java.lang.Long}
	    ) }
	</insert>

	<select id="checkUpdateIdDuplicate" resultType="int">
	    SELECT COUNT(*) 
	    FROM USERS 
	    WHERE BIZ_NO = #{bizNo} 
	      AND LOGIN = #{login} 
	      AND ID != #{id} 
	</select>
	      
	<update id="updateUser">
	    UPDATE USERS
	    SET
	    	LOGIN = #{login},
	        NAME = #{name},
	        TEL = #{tel},
	        EMAIL = #{email},
	        STATUS_CD = #{statusCd},
	        PRJ_MANAGER_CD = #{prjManagerCd},
	        GENDER_CD = #{genderCd},
	        HIRE_DATE = #{hireDate},
	        MCP_CD = #{mcpCd}
	    WHERE ID = #{id}
	    AND   BIZ_NO = #{bizNo}
	</update>
	
	<update id="bulkUpdateUsers" parameterType="map">
	    UPDATE USERS
	    <set>
	        <choose>
	            <when test="type == 'status'">
	                STATUS_CD = #{value}
	                <if test="value == '02ACTIVE'">
	                    , MCP_CD = '01ACTIVE'
	                </if>
	            </when>
	            
	            <when test="type == 'prjManager'">
	                PRJ_MANAGER_CD = #{value}
	            </when>
	        </choose>
	    </set>
	    WHERE 	BIZ_NO = #{bizNo}
	    AND		ID IN
	    <foreach item="id" collection="ids" open="(" separator="," close=")">
	        #{id}
	    </foreach>
	</update>
	
	</mapper>