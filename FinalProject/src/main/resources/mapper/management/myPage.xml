<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.management.mapper.myPage">

	<update id="updateUser" parameterType="UserVO">

		update users
		set
		EAML = #{email},
		TEL = #{tel},
		ADMIN_CD = #{adminCd},
		STATUS_CD = #{statusCd}

		<if test="R != null and password !=''">
			, password = #{password}
		</if>
		where id = #{id}
	</update>

</mapper>