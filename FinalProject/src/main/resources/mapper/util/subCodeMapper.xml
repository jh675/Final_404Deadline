<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper	namespace="com.example.demo.util.subCode.mapper.SubcodeMapper">
	<select id="getSubCodeList">
	SELECT  s_code,
	        m_code,
	        name,
	        note
	FROM    sub_code
	WHERE   m_code = #{mCode}
	</select>
	
	<select id="selectScodeNm">
	SELECT  name
	FROM	sub_code
	WHERE	s_code = #{sCode}
	</select>
</mapper>