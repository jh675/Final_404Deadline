<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.project.notice.mapper.NoticeMapper">

<select id="selectAll" resultType="com.example.demo.project.notice.service.NoticeVO">
    SELECT *
    FROM notice
    WHERE prj_id = #{prjId}
    ORDER BY  is_fixed,id DESC
</select>

<insert id="insert">
    INSERT INTO notice(id, prj_id, mem_id, title, description, is_fixed)
    VALUES(news_seq.nextval, #{prjId}, #{memId}, #{title}, #{description}, #{isFixed})
</insert>

<select id="selectOne" resultType="com.example.demo.project.notice.service.NoticeVO">
    SELECT * FROM notice WHERE id = #{id}
</select>

<update id="update">
    UPDATE notice
    SET  title = #{title}, 
         description = #{description}, 
         is_fixed = #{isFixed} 
    WHERE id = #{id}
</update>

<delete id="delete">
    DELETE FROM notice WHERE id = #{id}
</delete>

</mapper>